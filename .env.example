# Weather API — indianapi.in (https://indianapi.in/weather-api)
# Get your free key: https://indianapi.in/sign-in
VITE_WEATHER_API_KEY=

# Third-party Mandi Price API — keyless; source mirror provenance is not independently verified
VITE_MANDI_API_BASE=https://mandi-api.onrender.com/v1

# Vision Agent cloud fallback: server-side only (Vercel → Project Settings → Environment Variables).
# Use the dedicated endpoint supplied for this project; do not prefix these with VITE_.
ULTRALYTICS_ENDPOINT_URL=https://predict-6a8fe586becceb8c53b3b178-dproatj77a-el.a.run.app
ULTRALYTICS_API_KEY= # set in Vercel if the upstream endpoint requires authentication
# Optional comma-separated extra origins; same-origin site requests are already allowed.
ALLOWED_ORIGINS=
